# دليل نشر تطبيق «مع الله» على App Store و Google Play

التطبيق مكتوب بتقنيات الويب، ويُغلَّف بـ **Capacitor** ليصير تطبيق iOS و Android أصليًّا يُرفع للمتاجر.
الإشعارات (قبل الأذان، عند الأذان، أذكار الصباح والمساء، الورد) تُجدول في **نظام التشغيل نفسه**، فتصل والتطبيق مغلق تمامًا.

---

## ١) ما تحتاجه قبل البدء

| العنصر | التفصيل | التكلفة |
|---|---|---|
| جهاز **Mac** | Xcode لا يعمل إلا على macOS — شرط من آبل لا مفرّ منه | — |
| **Xcode** | من App Store على الماك (نسخة ١٥ فأحدث) | مجاني |
| **Node.js** | نسخة ٢٠ فأحدث — [nodejs.org](https://nodejs.org) | مجاني |
| **Apple Developer Program** | [developer.apple.com/programs](https://developer.apple.com/programs) | ٩٩ دولار / سنة |
| **Google Play Console** (اختياري) | لنشر نسخة أندرويد | ٢٥ دولار مرة واحدة |

> نسخة أندرويد تُبنى من نفس الكود على ويندوز أو ماك أو لينكس — الماك مطلوب لـ iOS فقط.

---

## ٢) تجهيز المشروع (مرة واحدة)

على جهاز الماك، افتح **Terminal**:

```bash
git clone https://github.com/saadaljammaz35-stack/-.git maa-allah
cd maa-allah
git checkout claude/islamic-app-maa-allah-7zlyyr

npm install          # تنزيل Capacitor وإضافاته
npm run add:ios      # إنشاء مشروع iOS
npm run add:android  # (اختياري) إنشاء مشروع أندرويد
```

بعد أي تعديل على التطبيق:

```bash
npm run sync         # يبني www/ وينسخها للمشروعين
```

---

## ٣) إعدادات مشروع iOS في Xcode

```bash
npm run ios          # يفتح المشروع في Xcode
```

داخل Xcode، اختر هدف **App** من الأعلى، ثم:

### أ) التوقيع والهوية — تبويب Signing & Capabilities
1. فعّل **Automatically manage signing**.
2. اختر **Team**: حسابك في Apple Developer.
3. **Bundle Identifier**: `sa.maaallah.app` — أو غيّره لما يخصّك (يجب أن يكون فريدًا عالميًّا).
4. اضغط **+ Capability** وأضف: **Push Notifications** ليس مطلوبًا، لكن أضف **Background Modes** ثم فعّل **Background fetch** (اختياري لتحديث المواقيت).

### ب) نصوص أذونات إلزامية — ملف `Info.plist`
آبل **ترفض التطبيق** إن طلب إذنًا بلا نصّ يشرح سببه. افتح `ios/App/App/Info.plist` وأضف داخل `<dict>`:

```xml
<key>NSLocationWhenInUseUsageDescription</key>
<string>يُستخدم موقعك لحساب مواقيت الصلاة واتجاه القبلة بدقة في مدينتك. لا يُرسل موقعك إلى أي خادم.</string>

<key>NSMotionUsageDescription</key>
<string>تُستخدم حسّاسات الجهاز لتشغيل بوصلة القبلة وتحديد اتجاه الكعبة.</string>

<key>CFBundleDisplayName</key>
<string>مع الله</string>

<key>CFBundleLocalizations</key>
<array>
  <string>ar</string>
</array>

<key>CFBundleDevelopmentRegion</key>
<string>ar</string>

<key>ITSAppUsesNonExemptEncryption</key>
<false/>
```

> السطر الأخير يوفّر عليك أسئلة «تصاريح التشفير» في كل رفعة.

### ج) الأيقونة وشاشة البداية
- الأيقونة الجاهزة: `assets/icon-512.png` و `assets/icon-maskable.png`.
- في Xcode: `App/Assets.xcassets/AppIcon` → اسحب صورة ١٠٢٤×١٠٢٤ (كبّر `icon-512.png` أو صمّم أدق).
- شاشة البداية: `App/Assets.xcassets/Splash` — استخدم خلفية `#faf8f2` مع الشعار في الوسط.

### د) الاتجاه واللغة
التطبيق عربي من اليمين لليسار بالكامل ولا يحتاج إعدادًا إضافيًّا — تأكّد فقط أن **Deployment Info → iPhone Orientation** فيه **Portrait** فقط.

---

## ٤) الرفع إلى App Store

1. في Xcode أعلى النافذة اختر الجهاز: **Any iOS Device (arm64)**.
2. من القائمة: **Product ← Archive** وانتظر انتهاء البناء.
3. في نافذة Organizer: **Distribute App ← App Store Connect ← Upload**.
4. افتح [appstoreconnect.apple.com](https://appstoreconnect.apple.com) ← **My Apps ← +** ← **New App**:
   - **Platform**: iOS
   - **Name**: مع الله
   - **Primary Language**: العربية
   - **Bundle ID**: نفس المعرّف أعلاه
   - **SKU**: `maaallah-001`

---

## ٥) بيانات المتجر (جاهزة للنسخ)

### الاسم
```
مع الله
```

### العنوان الفرعي (٣٠ حرفًا كحد أقصى)
```
أذكار ومواقيت وختمة القرآن
```

### الوصف
```
تطبيق إسلامي شامل على منهج أهل السنة والجماعة، يجمع لك ما تحتاجه في يومك:

• مواقيت الصلاة والشروق بحساب فلكي دقيق يعمل بلا إنترنت، مع ١٢ طريقة حساب معتمدة وأم القرى افتراضيًّا.
• تنبيه قبل الأذان بخمس دقائق (قابل للتغيير)، وتنبيه عند دخول الوقت.
• أذكار الصباح والمساء والنوم والاستيقاظ والصلاة والسفر والمنزل — كل ذكر بعدّاده ومصدره وفضله.
• أدعية من القرآن ومن دعاء النبي ﷺ، والرقية الشرعية، والاستغفار والتسابيح، وأدعية رمضان والعشر الأواخر.
• الأربعون النووية كاملة ومنتخبات من صحيحي البخاري ومسلم مرتّبة بأبوابها.
• ختمة القرآن: تختار عدد الأيام فيحسب لك ورد كل يوم، وكم بقي، وهل أنت متقدّم أم متأخّر.
• اتجاه القبلة ببوصلة حيّة تعمل بحسّاس الجهاز.
• شريط رمضان: كم بقي على الإفطار وكم على الإمساك، مع التاريخ الهجري واليوم من الشهر.
• بث مباشر من المسجد الحرام والمسجد النبوي.
• المسبحة الإلكترونية، وأسماء الله الحسنى بمعانيها، وبحث في كل النصوص.
• وضع ليلي ونهاري، وحجم خط قابل للتكبير.

كل نصوص الأذكار والأدعية والأحاديث منسوبة إلى مصادرها من الكتاب والسنة الصحيحة.

بلا حساب، بلا تتبّع، بلا إعلانات — وبياناتك تبقى على جهازك وحده.
```

### الكلمات المفتاحية (١٠٠ حرف)
```
أذكار,أدعية,مواقيت الصلاة,القبلة,القرآن,ختمة,حصن المسلم,رمضان,أذان,تسبيح,الأربعون النووية
```

### لقطات الشاشة المطلوبة
| المقاس | المطلوب |
|---|---|
| ٦٫٩ بوصة (١٢٩٠×٢٧٩٦) | ٣ لقطات على الأقل — إجباري |
| ٦٫٥ بوصة (١٢٤٢×٢٦٨٨) | إجباري |

التقطها من محاكي Xcode: **iPhone 16 Pro Max** ← `⌘S`. أفضل شاشات للعرض: الرئيسية، مواقيت الصلاة، الختمة، أذكار الصباح، القبلة.

### سياسة الخصوصية
إلزامية ومطلوب لها **رابط عام**. الملف جاهز في `PRIVACY.md` — انشره على GitHub Pages وضع رابطه:
```
https://saadaljammaz35-stack.github.io/-/PRIVACY.html
```

### خصوصية البيانات (App Privacy)
في App Store Connect ← **App Privacy** اختر: **Data Not Collected** — التطبيق فعلًا لا يجمع شيئًا.

### التصنيف العمري
`4+` — بلا محتوى حسّاس.

---

## ٦) تفادي الرفض (مهم جدًّا)

آبل ترفض بموجب **القاعدة 4.2 (Minimum Functionality)** التطبيقات التي هي «مجرد موقع مغلّف». تطبيقك **ليس كذلك**، فاكتب هذا في خانة **App Review Notes**:

```
التطبيق يعمل بالكامل دون اتصال بالإنترنت: مواقيت الصلاة تُحسب فلكيًّا على الجهاز،
وكل نصوص الأذكار والأدعية والأحاديث مخزّنة محليًّا داخل التطبيق.

يستخدم التطبيق وظائف أصلية للنظام:
- إشعارات محلية مجدولة في نظام iOS للتنبيه قبل الأذان وعنده وللأذكار اليومية.
- حسّاسات الجهاز (المغناطيسية) لتشغيل بوصلة القبلة.
- خدمة الموقع لحساب المواقيت واتجاه القبلة محليًّا.

لا يجمع التطبيق أي بيانات ولا يتطلّب حسابًا، وجميع بيانات المستخدم تبقى على جهازه.
```

**نقاط تُسبّب الرفض — تجنّبها:**
- ❌ ترك أذونات بلا نصوص شرح في `Info.plist`.
- ❌ رابط سياسة خصوصية معطّل.
- ❌ روابط تبرّع تفتح دفعًا داخل التطبيق — روابط «إحسان» في التطبيق تفتح **المتصفح الخارجي**، وهذا مقبول لأنها جهة خيرية مسجّلة، لكن إن اعترضت آبل فاحذف الشاشة أو حوّلها إلى نصّ تعريفي فقط.
- ❌ شاشة البث المباشر إن توقّفت القناة — تأكّد أنها تعمل قبل الإرسال للمراجعة.

المراجعة تستغرق عادة **من يوم إلى ثلاثة أيام**.

---

## ٧) نسخة أندرويد (Google Play)

```bash
npm run android      # يفتح Android Studio
```

1. في `android/app/build.gradle` ارفع `versionCode` و `versionName` مع كل إصدار.
2. أنشئ مفتاح التوقيع:
   ```bash
   keytool -genkey -v -keystore maa-allah.keystore -alias maaallah \
     -keyalg RSA -keysize 2048 -validity 10000
   ```
   > **احفظ هذا الملف وكلمة المرور في مكان آمن** — فقدانه يعني أنك لن تستطيع تحديث التطبيق أبدًا.
3. في Android Studio: **Build ← Generate Signed Bundle / APK ← Android App Bundle**.
4. ارفع ملف `.aab` في [Play Console](https://play.google.com/console).

أذونات أندرويد مضبوطة تلقائيًّا من الإضافات، وتحتاج فقط تأكيد `POST_NOTIFICATIONS` في `AndroidManifest.xml` (يضيفه Capacitor).

---

## ٨) التحديثات لاحقًا

```bash
git pull
npm run sync
```
ثم ارفع رقم الإصدار في Xcode (**Version** و **Build**) وكرّر خطوة Archive.

---

## بديل مجاني بلا ماك ولا رسوم

إن أردت النشر فورًا بلا تكلفة: ارفع التطبيق على **GitHub Pages** وشارك الرابط.
من يفتحه بالجوال يضيفه للشاشة الرئيسية فيعمل كتطبيق كامل دون إنترنت — الفرق الوحيد أن الإشعارات على آيفون تصل فقط والتطبيق مفتوح أو في الخلفية، لا وهو مغلق تمامًا.
